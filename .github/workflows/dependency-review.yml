name: Dependency Review

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: dependency-review-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  review:
    permissions:
      contents: read
      pull-requests: read
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-dependency-review.yml@8533c21000bd69bcb1c1ab480574e50140b9add8
