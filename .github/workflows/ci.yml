name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  baseline:
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-python-baseline.yml@85256537aac01769cee6d5614c7b0b68a0191435
