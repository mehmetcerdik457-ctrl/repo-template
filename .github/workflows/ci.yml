name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  baseline:
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-python-baseline.yml@8533c21000bd69bcb1c1ab480574e50140b9add8
