name: Privacy Guard

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: privacy-guard-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  guard:
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-privacy-guard.yml@85256537aac01769cee6d5614c7b0b68a0191435
