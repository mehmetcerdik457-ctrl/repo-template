name: Privacy Guard

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: privacy-guard-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  guard:
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-privacy-guard.yml@8533c21000bd69bcb1c1ab480574e50140b9add8
